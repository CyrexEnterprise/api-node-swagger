/ping:
  get:
    tags:
      - meta
    description: Returns 'pong' to the caller
    operationId: ping
    responses:
      204:
        $ref: "#/responses/noContent"
      default:
        $ref: "#/responses/unexpected"
  head:
    tags:
      - meta
    description: Returns 'pong' to the caller
    operationId: ping
    responses:
      204:
        $ref: "#/responses/noContent"
      default:
        $ref: "#/responses/unexpected"
