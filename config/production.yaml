oauth2:
  logger:
    middleware: false

caller:
  queue:
    name: rpc

api:
  logger:
    middleware: false
    transports:
      Console: false
  swagger:
    outputSpec: ./api-docs.json
    ui: false
    serveDocs: /api-docs

superadmin:
  logger:
    middleware: false
  swagger:
    outputSpec: ./sa-api-docs.json
    ui: false
    serveDocs: false
